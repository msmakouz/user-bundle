{% extends '@MainBundle/admin/layouts/base.html.twig' %}

{% block title %}{{ 'zentlix_user.group.groups' | trans }}{% endblock %}

{% block breadcrumbs %}
    <li class="breadcrumb-item"><a href="{{ path('admin.index') }}">{{ 'zentlix_main.control_panel' | trans }}</a></li>
    <li class="breadcrumb-item active">{{ 'zentlix_user.group.groups' | trans }}</li>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="card w-100">
            <div class="card-header">
                {{ 'zentlix_user.group.groups' | trans }}
                <a href="{{ path('admin.group.create') }}" class="btn btn-primary btn-sm float-right">
                    {{ 'zentlix_user.group.create.action' | trans }}
                </a>
            </div>
            <div class="card-body">
                <div id="groups-datatable">{{ 'zentlix_main.loading_dots' | trans }}</div>
                <script>
                    $(document).ready(function () {
                        $('#groups-datatable').initDataTables({{ datatable_settings(datatable) }});
                    });
                </script>
            </div>
        </div>
    </div>
{% endblock %}