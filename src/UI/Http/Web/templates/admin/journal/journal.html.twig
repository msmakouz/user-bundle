{% extends '@MainBundle/admin/layouts/base.html.twig' %}

{% block title %}{{ 'zentlix_user.authorization.journal' | trans }}{% endblock %}

{% block breadcrumbs %}
    <li class="breadcrumb-item"><a href="{{ path('admin.index') }}">{{ 'zentlix_main.control_panel' | trans }}</a></li>
    <li class="breadcrumb-item active">{{ 'zentlix_user.authorization.journal' | trans }}</li>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="card w-100">
            <div class="card-header">{{ 'zentlix_user.authorization.journal' | trans }}</div>
            <div class="card-body">
                <div id="journal-datatable">{{ 'zentlix_main.loading_dots' | trans }}</div>
                <script>
                    $(document).ready(function () {
                        $('#journal-datatable').initDataTables({{ datatable_settings(datatable) }});
                    });
                </script>
            </div>
        </div>
    </div>
{% endblock %}